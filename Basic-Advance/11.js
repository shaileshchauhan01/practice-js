// Prompt two numbers from user and store them in variable numA and numB, then declare new var sum and add both number using + operator and alert the value.

let numA = Number(prompt("Enter first number"));
let numB = Number(prompt("Enter second number"));

let sum = numA + numB;
alert(`Sum of first and second number is ${sum}`);
