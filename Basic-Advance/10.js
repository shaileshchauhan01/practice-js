// Declare a variable user and prompt the name from user, display the value using alert.
// Declare a new var username , asign the user value to it and console both user and username 
// Declare a var age and isLearning with value 22 and true, in same line. 
// Change the value of user into John and console both user ans udername.

let user = String(prompt("Enter your name"))
alert(user);

let username = user;
console.log(user, username);

let age = 22, isLearning = true;

user = "John";
console.log(user, username);